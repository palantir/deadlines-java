buildscript {
    repositories {
        mavenCentral()
        gradlePluginPortal()
    }

    dependencies {
        classpath 'com.palantir.baseline-error-prone:gradle-baseline-error-prone:0.26.0'
        classpath 'com.palantir.gradle.failure-reports:gradle-failure-reports:1.19.0'
        classpath 'com.palantir.jakartapackagealignment:jakarta-package-alignment:0.6.0'
        classpath 'com.palantir.gradle.jdks:gradle-jdks:0.82.0'
        classpath 'com.palantir.gradle.jdkslatest:gradle-jdks-latest:0.31.0'
        classpath 'com.palantir.javaformat:gradle-palantir-java-format:2.96.0'
        classpath 'com.palantir.suppressible-error-prone:gradle-suppressible-error-prone:2.31.0'
        classpath 'com.palantir.gradle.consistentversions:gradle-consistent-versions:3.18.0'
        classpath 'com.palantir.baseline:gradle-baseline-java:7.5.0'
        classpath 'com.palantir.gradle.externalpublish:gradle-external-publish-plugin:1.36.0'
        classpath 'com.palantir.gradle.gitversion:gradle-git-version:5.0.0'
        classpath 'com.palantir.gradle.idea-configuration:gradle-idea-configuration:0.11.0'
        classpath 'com.palantir.metricschema:gradle-metric-schema:0.43.0'
        classpath 'com.palantir.gradle.revapi:gradle-revapi:2.3.0'
    }
}

apply plugin: 'com.palantir.external-publish'
apply plugin: 'com.palantir.git-version'
apply plugin: 'com.palantir.jdks'
apply plugin: 'com.palantir.consistent-versions'
apply plugin: 'com.palantir.baseline'
apply plugin: 'com.palantir.baseline-error-prone-root'
apply plugin: 'com.palantir.baseline-java-versions'
apply plugin: 'com.palantir.jdks.latest'
apply plugin: 'com.palantir.failure-reports'

javaVersions {
    javaCompiler = 25
    libraryTarget = 17
    runtime = 25
}

version gitVersion()

allprojects {
    repositories {
        mavenCentral() { metadataSources { mavenPom(); ignoreGradleMetadataRedirection() } }
    }

    apply plugin: 'com.palantir.java-format'
    apply plugin: 'com.palantir.baseline-null-away'
    apply plugin: 'com.palantir.jakarta-package-alignment'

    group 'com.palantir.deadlines'
    version System.env.CIRCLE_TAG ?: rootProject.version
}

jdks {
    daemonTarget = 21
}
